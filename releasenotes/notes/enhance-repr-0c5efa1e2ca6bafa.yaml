---
enhancements:
  - |
    Customize `Pipeline.__repr__()` to return a nice text representation of it.
    If run on a Jupyter notebook it will instead embed the image created with `Pipeline.draw()`.
