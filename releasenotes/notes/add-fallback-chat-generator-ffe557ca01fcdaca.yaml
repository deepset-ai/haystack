---
highlights: >
  Introduced `FallbackChatGenerator` to orchestrate sequential retries across chat providers, bolstering production reliability, tightening latency budgets, and preserving answer guarantees during provider outages.
features:
  - |
    Added `FallbackChatGenerator`, enabling pipelines to automatically retry multiple chat generators and return the first successful response with rich metadata about the attempted fallbacks.
