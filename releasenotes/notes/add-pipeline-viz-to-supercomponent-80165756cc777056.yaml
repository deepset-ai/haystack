---
highlights: >
  Adding visualization capabilities to SuperComponent via the following methods:
  - `show()` displays pipeline diagrams in Jupyter notebooks.
  - `draw()` saves pipeline diagrams as images to specified file paths.
