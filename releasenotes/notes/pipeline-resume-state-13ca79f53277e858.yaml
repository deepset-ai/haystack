---
features:
  - |
    A Pipeline can now be resumed from its last successful step. The Pipeline.run() method a new parameter `state_persistence`
    which if set to `True` enables state persistence, meaning that Pipeline snapshots would be saved to the the path specified
    in `state_persistence_path`. Any pipeline snapshot can be used to resume a pipeline execution.
