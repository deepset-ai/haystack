---
features:
  - |
    Add `run_async` method to HuggingFaceAPIChatGenerator. This method internally AsyncInferenceClient from huggingface
    to generate chat completions and supports the same parameters as the `run` method. It returns a coroutine
    that can be awaited.
