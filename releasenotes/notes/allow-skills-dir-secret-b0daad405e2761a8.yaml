---
enhancements:
  - |
    ``FileSystemSkillStore``'s ``skills_dir`` now also accepts a ``Secret``, so the skills directory path can be
    sourced from an environment variable (e.g. ``Secret.from_env_var("SKILLS_DIR")``) instead of being hard-coded
    in pipeline configuration.
