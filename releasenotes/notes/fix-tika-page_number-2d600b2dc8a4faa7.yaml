---
fixes:
  - |
    Fix Tika not providing `\f` tag for page number tracking by integrating Haystack 1.X integration of Tika logic.
    It uses Tika parsing to HTML and then process the HTML to insert new page (`\f`) tag.
