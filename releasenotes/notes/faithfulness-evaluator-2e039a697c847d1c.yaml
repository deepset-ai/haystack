---
features:
  - |
    Add a new FaithfulnessEvaluator component that can be used to evaluate hallucinations of LLMs in a RAG pipeline.
    Given a question, a list of retrieved document contents, and a predicted answer, FaithfulnessEvaluator returns 1 if the answer can be inferred from the documents, otherwise 0.
