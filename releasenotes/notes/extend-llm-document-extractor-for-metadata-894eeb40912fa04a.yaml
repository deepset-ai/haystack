---
enhancements:
  - |
    The ``LLMDocumentContentExtractor`` now also supports metadata extraction. It can run in two modes (content or metadata), chosen at init and optionally overridden at runtime via ``run(..., extraction_mode=...)``.
