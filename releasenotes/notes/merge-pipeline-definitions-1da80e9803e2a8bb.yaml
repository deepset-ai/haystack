---
enhancements:
  - |
    Merge `Pipeline`s definitions into a single `Pipeline` class.
    The class in `haystack.pipeline` package has been deleted and only the `haystack.core.pipeline` exist now.
